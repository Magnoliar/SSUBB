# SSUBB Worker 依赖
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
httpx>=0.27.0
pyyaml>=6.0

# ASR (转写): 使用 faster-whisper-xxl 独立二进制，无需 Python 依赖
# 下载: https://github.com/Purfview/whisper-standalone-win/releases

# 翻译 (LLM)
openai>=1.50.0

# 工具
python-multipart>=0.0.9
